On the ingest page we currently accept a file path to a PRD file. 
We also want to allow users to paste the contents of a PRD directly into 
the ingest form using a Shadcn textarea component so that ingestion can be read from the entered text
or the existing PRD path.
